CREATE TABLE PRODUCTO (ID INTEGER NOT NULL, DESCRIPCION VARCHAR(255), NOMBRE VARCHAR(255), PRECIO FLOAT, PRIMARY KEY (ID))
CREATE TABLE CARRITO (ID INTEGER NOT NULL, CANTIDAD INTEGER, PRODUCTO_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE CARRITO ADD CONSTRAINT CARRITOPRODUCTO_ID FOREIGN KEY (PRODUCTO_ID) REFERENCES PRODUCTO (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN_TABLE', 0)
